{ # '0
    let x = 1
    io.print("Scope '0: x=" + x) # >>> 1

    { # '1
        io.print("Scope '1: x=" + x) # >>> 1
        let x = -1 # this does not affect the x of the outer scope
        io.print("Scope '1: x=" + x) # >>> -1
    }

    io.print("Scope '0: x=" + x) # >>> 1
}
