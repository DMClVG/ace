{
    print("What is A?")
    a = input()
    parsedA = parse(a) # parse a

    a = if parsedA != nil {
        parsedA
    } else { a }

    print("What is B?")

    b = input()
    parsedB = parse(b)
    b = if parsedB != nil {
        parsedB
    } else { b }

    print("A + B is " + (a + b))
}