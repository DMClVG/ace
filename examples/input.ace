{
    io.print("What is A?")
    a = io.input()
    parsedA = std.parse(a) # parse a
    
    a = if parsedA != nil {
        parsedA
    } else { a }

    io.print("What is B?")

    b = io.input()
    parsedB = std.parse(b)
    b = if parsedB != nil {
        parsedB
    } else { b }

    io.print("A + B is " + (a + b))
}