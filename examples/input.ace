io.print("What is A?")
a = io.input()
parsedA = std.parse(a)

if parsedA != nil {
    a = parsedA
}

io.print("What is B?")

b = io.input()
parsedB = std.parse(b)
if parsedB != nil {
    b = parsedB
}

io.print("A + B is " + (a + b))
