print("What is A?")
let a = io.input()
let parsedA = std.parse(a)

if parsedA != nil {
    a = parsedA
}

print("What is B?")

let b = io.input()
let parsedB = std.parse(b)
if parsedB != nil {
    b = parsedB
}

print("A + B is " + (a + b))
